from setuptools import setup

setup(name='guess_number_oop',
      version='0.1',
      description='Guess a random computer-generated number (OOP practice)',
      packages=['guess_number_oop'],
      zip_safe=False)
